# Environment Configuration - Railway Production
# Set these as environment variables in Railway dashboard

# Environment (CRITICAL - must be production)
APP_ENV=production
DEV_BYPASS_AUTH=false  # MUST be false in production (hard override enforced)

# Database (Railway PostgreSQL or Supabase)
DATABASE_URL=${DATABASE_URL}  # Railway provides this automatically

# Redis (Railway Redis addon)
REDIS_URL=${REDIS_URL}  # Railway provides this if Redis addon is added

# Security (CRITICAL - use strong values)
SECRET_KEY=${SECRET_KEY}  # Generate with: openssl rand -hex 32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS (comma-separated production origins)
CORS_ORIGINS=https://your-frontend-domain.com,https://www.your-frontend-domain.com

# Application
APP_NAME=CyberCafe POS Pro
APP_VERSION=1.0.0
DEBUG=false  # MUST be false in production

# M-Pesa Daraja (Production)
MPESA_CONSUMER_KEY=${MPESA_CONSUMER_KEY}
MPESA_CONSUMER_SECRET=${MPESA_CONSUMER_SECRET}
MPESA_SHORTCODE=${MPESA_SHORTCODE}
MPESA_PASSKEY=${MPESA_PASSKEY}
MPESA_ENVIRONMENT=production
MPESA_CALLBACK_URL=https://your-backend-domain.railway.app/mpesa/callback
MPESA_ALLOWED_IPS=196.201.214.200,196.201.214.206  # Safaricom production IPs

# File Uploads
UPLOAD_DIR=/app/uploads  # Railway persistent storage
MAX_UPLOAD_SIZE_MB=10
ALLOWED_UPLOAD_TYPES=application/pdf,image/jpeg,image/png,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document

# Health Check
HEALTH_CHECK_TOKEN=${HEALTH_CHECK_TOKEN}  # Optional monitoring token
